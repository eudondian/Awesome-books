// Book Class

// UI class
class UI {
    static displayBooks() {
  
      const books = StoredBooks;
  
  books.forEach((bok) => UI.addBookToList(book);
  
    }
  }
  
  // Event:  Add a Book
  
  // Event: Remove a Book
  